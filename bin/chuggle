#!/usr/bin/env ruby
require 'chuggle'
require 'pp'
unless ARGV.first && File.directory?(ARGV.first)
  STDERR.puts "Usage:"
  STDERR.puts "chuggle /path/to/git/repo/with/ruby/files"
  exit
end
pp Chuggle.new(ARGV.first).metrics
